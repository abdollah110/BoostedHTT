This is the steps to run the svfit on already-skimmed samples:

https://gitlab.cern.ch/KState-HEP-HTT/SubmitSVFit/-/tree/FastMTT


cmsrel CMSSW_9_4_4 
cd CMSSW_9_4_4/src/
cmsenv
git clone -b CMSSW_9_4_4_classic_svFit_v0 ssh://git@gitlab.cern.ch:7999/KState-HEP-HTT/SubmitSVFit.git
cd SubmitSVFit
source recipe.sh
emacs -nw TauAnalysis/ClassicSVfit/src/ClassicSVfit.cc
# comment out line 38 (delete histogramAdapter_;) to avoid seg. fault
scram b -j 8

// One need to copy the updated FastMTT.cc in the bin directory:

 
cp  FastMTT.cc $CMSSW_BASE/src/SubmitSVFit/ROOT/bin/.

and make sure it is linked in the BuildFile.xml

<bin   name="FastMTT" file="FastMTT.cc">

cd $CMSSW_BASE/src

scram b -j 8

Here are the steps for submitting the SVFit:


1) List all samples you intend to make skim (from /store/.... )
2) run the makeSubmissionPackage.py  script:
	########
	python makeSubmissionPackage.py -x FastMTT -i InputSample.txt -n muTau_v1 -o /store/user/abdollah/SVFit/mt/v1
	########

At the end this
